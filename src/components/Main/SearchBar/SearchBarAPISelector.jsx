import { styled } from "styled-components";

const SearchBarAPISelector = styled.select`
    width: 150px;
    padding: 5px;
    border-radius: 10px;
    text-align: center;
    border: 1px solid rgba(0,0,0,0.3);
    box-shadow:0 0 15px 4px rgba(0,0,0,0.08);
    color: rgba(0,0,0,0.6);
`;

export default SearchBarAPISelector;
